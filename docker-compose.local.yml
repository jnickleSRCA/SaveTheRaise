services:
  api:
    environment:
      CORS_ORIGIN: http://localhost:15460,http://ubuntu02:15460

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        NEXT_PUBLIC_API_URL: http://localhost:15461
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:15461
